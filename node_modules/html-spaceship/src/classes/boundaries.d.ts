import { XY } from '../../types/interfaces';
import Entity from './Entity';
export default class Boundary extends Entity {
    el: HTMLElement;
    center: XY;
    constructor(el: HTMLElement);
    update(): void;
}
export declare class CircleBoundary extends Boundary {
    readonly kind: 'circle';
    readonly radius: number;
    constructor(el: HTMLElement);
}
export declare class RectBoundary extends Boundary {
    readonly kind: 'rect';
    vertices: XY[];
    constructor(el: HTMLElement);
    update(): void;
}
