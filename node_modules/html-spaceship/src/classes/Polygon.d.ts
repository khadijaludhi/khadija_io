import { XY } from '../../types/interfaces';
export default class Polygon {
    vertices: XY[];
    private _oldTheta;
    center: XY;
    constructor(vertices: XY[], center: XY);
    rotate(theta: number, origin: XY): void;
    updateXPosition(shift: number): void;
    updateYPosition(shift: number): void;
}
