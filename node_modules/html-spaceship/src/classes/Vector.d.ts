import { XY } from '../../types/interfaces';
export default class Vector {
    x: number;
    y: number;
    constructor(x: number, y: number);
    static fromPoints(point1: XY, point2: XY): Vector;
    static fromVector(vector: Vector): Vector;
    dot(v2: XY): number;
    getCrossProduct(vector: Vector): number;
    toLeftNormal(): this;
    toRightNormal(): this;
    getMagnitude(): number;
    normalize(): this;
    multiply(scalar: number): this;
}
